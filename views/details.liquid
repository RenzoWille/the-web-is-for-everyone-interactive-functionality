{% render 'partials/head.liquid' %}


<article class="title-block">
    {% for api in artworkData %}
        {% if api.id == api.id  %}
        <li>
            <h1>{{ api.title }}</h1>
            <img src="{{ api.image }}" alt="art images for qatar museums">
        </li>
        {% endif %}
        {% endfor %}
</article>